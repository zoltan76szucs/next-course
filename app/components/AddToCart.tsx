"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("Button click")}> Add to card</button>
    </div>
  );
};

export default AddToCart;
